# AUTO-GENERATED MERGED YAML
# You can check at module/platform/development/paradigm/concurrency/thread/src/main/resources/application.yml
# Source: THREAD
# Dependencies: SPRING_WEB, AZURE_TRANSLATE

---
spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
    mvc:
      async:
        request-timeout: 600000
  application:
    name: ${PROJECT_NAME}
server:
  tomcat:
    connection-timeout: 600000
  port: ${PORT}
  servlet:
    context-path: /${CONTEXT_PATH}
swagger:
  enabled: ${BUILD_SWAGGER:}
  languages: ${BUILD_SWAGGER_LANGUAGE_LIST:}
springdoc:
  swagger-ui:
    enabled: ${swagger.enabled}
    docExpansion:
      - none
  api-docs:
    enabled: ${swagger.enabled}
---
spring:
  config:
    activate:
      on-profile: dev
---
spring:
  config:
    activate:
      on-profile: loc
---
spring:
  config:
    activate:
      on-profile: prd
swagger:
  enabled: false
