# <quiz-schema>
# Quiz Question Structure
# schema-version: 3
#
# questions [type: list; required; default: []; unique by: id]
#   Quiz questions for the current module language.
#   each item:
#     id [type: string; required; default: ""]
#       Stable and unique identity of the question.
#     question [type: string; required; default: ""]
#       Question text displayed to the learner.
#     aiGenerated [type: boolean; required; default: true]
#       Marks whether the question was generated by AI. Defaults to true for new content.
#     reviewed [type: boolean; required; default: false]
#       Marks whether the question has been human-reviewed and corrected. Defaults to false.
#     readmeRelated [type: object; required; default: {file: "", anchor: ""}]
#       Optional link to a Knowledge section in README. Both file and anchor may remain blank.
#       file [type: string; required; default: ""]
#         README path relative to readme/{language}/menu, for example 1.Basic/Basic.md.
#       anchor [type: string; required; default: ""]
#         Exact Knowledge section id in the specified README file.
#     apiRelated [type: object; required; default: {controller: "", methodSignature: ""}]
#       Optional link to an active API in Swagger metadata. Both controller and methodSignature may remain blank.
#       controller [type: string; required; default: ""]
#         Exact controller name, for example BasicThreadController.
#       methodSignature [type: string; required; default: ""]
#         Exact method signature in api-descriptions.yml, for example startVsRun().
#     answers [type: list; required; item count: 4; default: []; unique by: id]
#       Exactly four answers. A/B/C/D are stable internal identities, not display positions after frontend shuffling.
#       each item:
#         id [type: enum; required; allowed values: A, B, C, D; default: ""]
#           Stable answer identity.
#         answer [type: string; required; default: ""]
#           Answer text displayed to the learner.
#         explanation [type: string; required; default: ""]
#           Explanation shown when this answer is selected, whether it is correct or incorrect.
#     correctAnswerId [type: enum; required; allowed values: A, B, C, D; default: ""]
#       Internal id of the correct answer. The frontend checks this identity after shuffling.
# </quiz-schema>

questions:
  - id: integer-overflow-wrap
    question: "What does `Integer.MAX_VALUE + 1` do in ordinary Java int arithmetic?"
    answers:
      - {id: A, answer: "It wraps to Integer.MIN_VALUE without automatically throwing.", explanation: "Correct. Primitive integer overflow wraps according to fixed-width signed integer semantics unless an exact helper is used."}
      - {id: B, answer: "It becomes a long automatically.", explanation: "Incorrect. Primitive integer overflow wraps according to fixed-width signed integer semantics unless an exact helper is used."}
      - {id: C, answer: "It throws ArithmeticException automatically.", explanation: "Incorrect. Primitive integer overflow wraps according to fixed-width signed integer semantics unless an exact helper is used."}
      - {id: D, answer: "It saturates at Integer.MAX_VALUE.", explanation: "Incorrect. Primitive integer overflow wraps according to fixed-width signed integer semantics unless an exact helper is used."}
    correctAnswerId: A
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "2.IntegerOverflow/IntegerOverflow.md", anchor: "integer-overflow-wraparound"}
    apiRelated: {controller: "IntegerArithmeticController", methodSignature: "overflow()"}

  - id: integer-add-exact
    question: "When is `Math.addExact` preferable to ordinary `+` for ints?"
    answers:
      - {id: A, answer: "When all operands are floating point.", explanation: "Incorrect. The exact helpers throw ArithmeticException when the result cannot be represented."}
      - {id: B, answer: "When overflow must be detected instead of silently wrapping.", explanation: "Correct. The exact helpers throw ArithmeticException when the result cannot be represented."}
      - {id: C, answer: "When overflow should clamp to MAX_VALUE.", explanation: "Incorrect. The exact helpers throw ArithmeticException when the result cannot be represented."}
      - {id: D, answer: "Only when using BigDecimal.", explanation: "Incorrect. The exact helpers throw ArithmeticException when the result cannot be represented."}
    correctAnswerId: B
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "2.IntegerOverflow/IntegerOverflow.md", anchor: "checked-arithmetic"}
    apiRelated: {controller: "IntegerArithmeticController", methodSignature: "overflow()"}

  - id: floating-decimal-approx
    question: "Why can `0.1 + 0.2` differ from exact decimal 0.3?"
    answers:
      - {id: A, answer: "The JVM randomly perturbs double values.", explanation: "Incorrect. IEEE 754 binary floating point represents many decimal fractions approximately."}
      - {id: B, answer: "Addition uses String conversion internally.", explanation: "Incorrect. IEEE 754 binary floating point represents many decimal fractions approximately."}
      - {id: C, answer: "Most decimal fractions do not have finite binary floating-point representations, so operations use nearby representable values.", explanation: "Correct. IEEE 754 binary floating point represents many decimal fractions approximately."}
      - {id: D, answer: "double stores decimal digits exactly but println changes them.", explanation: "Incorrect. IEEE 754 binary floating point represents many decimal fractions approximately."}
    correctAnswerId: C
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "3.FloatingPoint/FloatingPoint.md", anchor: "precision-rounding-error"}
    apiRelated: {controller: "FloatingPointController", methodSignature: "precision()"}

  - id: nan-comparison
    question: "What is the correct way to test whether a double is NaN?"
    answers:
      - {id: A, answer: "Use `value == Double.NaN`.", explanation: "Incorrect. NaN is unordered and even `NaN == NaN` is false."}
      - {id: B, answer: "Use `value > Double.MAX_VALUE`.", explanation: "Incorrect. NaN is unordered and even `NaN == NaN` is false."}
      - {id: C, answer: "Use `value == null`.", explanation: "Incorrect. NaN is unordered and even `NaN == NaN` is false."}
      - {id: D, answer: "Use `Double.isNaN(value)`, not equality with `Double.NaN`.", explanation: "Correct. NaN is unordered and even `NaN == NaN` is false."}
    correctAnswerId: D
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "3.FloatingPoint/FloatingPoint.md", anchor: "nan-infinity-negative-zero"}
    apiRelated: {controller: "FloatingPointController", methodSignature: "specialValues()"}

  - id: biginteger-immutable
    question: "What happens if `a.add(BigInteger.ONE)` is called and the return value is ignored?"
    answers:
      - {id: A, answer: "`a` is unchanged because BigInteger is immutable and arithmetic returns a new value.", explanation: "Correct. BigInteger operations never mutate the receiver."}
      - {id: B, answer: "a increments in place.", explanation: "Incorrect. BigInteger operations never mutate the receiver."}
      - {id: C, answer: "The JVM mutates a only when it is final.", explanation: "Incorrect. BigInteger operations never mutate the receiver."}
      - {id: D, answer: "The call is illegal because BigInteger has no add method.", explanation: "Incorrect. BigInteger operations never mutate the receiver."}
    correctAnswerId: A
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "4.BigInteger/BigInteger.md", anchor: "big-integer-immutability"}
    apiRelated: {controller: "", methodSignature: ""}

  - id: bigdecimal-double-constructor
    question: "Why is `new BigDecimal(0.1)` often surprising?"
    answers:
      - {id: A, answer: "BigDecimal rounds every input to an integer.", explanation: "Incorrect. Construct from decimal String or use valueOf when decimal intent comes from a double literal."}
      - {id: B, answer: "It captures the exact binary double value, not the human decimal literal 0.1.", explanation: "Correct. Construct from decimal String or use valueOf when decimal intent comes from a double literal."}
      - {id: C, answer: "The constructor always returns null.", explanation: "Incorrect. Construct from decimal String or use valueOf when decimal intent comes from a double literal."}
      - {id: D, answer: "BigDecimal cannot represent decimal fractions.", explanation: "Incorrect. Construct from decimal String or use valueOf when decimal intent comes from a double literal."}
    correctAnswerId: B
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "5.BigDecimal/BigDecimal.md", anchor: "big-decimal-construction"}
    apiRelated: {controller: "BigDecimalController", methodSignature: "construction()"}

  - id: bigdecimal-nonterminating-division
    question: "What happens with `BigDecimal.ONE.divide(new BigDecimal(\"3\"))` without a rounding policy?"
    answers:
      - {id: A, answer: "It silently returns 0.33.", explanation: "Incorrect. BigDecimal division is exact unless a precision or rounding policy is supplied."}
      - {id: B, answer: "It returns positive infinity.", explanation: "Incorrect. BigDecimal division is exact unless a precision or rounding policy is supplied."}
      - {id: C, answer: "It throws ArithmeticException because the decimal expansion is non-terminating.", explanation: "Correct. BigDecimal division is exact unless a precision or rounding policy is supplied."}
      - {id: D, answer: "It converts to double automatically.", explanation: "Incorrect. BigDecimal division is exact unless a precision or rounding policy is supplied."}
    correctAnswerId: C
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "5.BigDecimal/BigDecimal.md", anchor: "big-decimal-arithmetic"}
    apiRelated: {controller: "", methodSignature: ""}

  - id: precision-vs-scale
    question: "For `new BigDecimal(\"123.4500\")`, what do precision and scale describe?"
    answers:
      - {id: A, answer: "Both always equal the number of digits after the decimal point.", explanation: "Incorrect. Precision and scale are distinct representation properties and should not be conflated."}
      - {id: B, answer: "Precision is always 2 for money.", explanation: "Incorrect. Precision and scale are distinct representation properties and should not be conflated."}
      - {id: C, answer: "Scale is the number of bytes used in memory.", explanation: "Incorrect. Precision and scale are distinct representation properties and should not be conflated."}
      - {id: D, answer: "Precision counts significant digits; scale records decimal-place positioning, which is 4 here.", explanation: "Correct. Precision and scale are distinct representation properties and should not be conflated."}
    correctAnswerId: D
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "6.PrecisionScale/PrecisionScale.md", anchor: "precision-vs-scale"}
    apiRelated: {controller: "BigDecimalController", methodSignature: "precisionScale()"}

  - id: rounding-domain-policy
    question: "Which statement about financial rounding is most accurate?"
    answers:
      - {id: A, answer: "The correct mode and boundary come from the domain rule; there is no universal two-decimal HALF_UP policy for all money calculations.", explanation: "Correct. Tax, interest, conversion, line items, and settlement may require different policies."}
      - {id: B, answer: "HALF_UP is legally correct for every currency and domain.", explanation: "Incorrect. Tax, interest, conversion, line items, and settlement may require different policies."}
      - {id: C, answer: "Rounding should always occur after every arithmetic operation.", explanation: "Incorrect. Tax, interest, conversion, line items, and settlement may require different policies."}
      - {id: D, answer: "Rounding mode never affects final totals.", explanation: "Incorrect. Tax, interest, conversion, line items, and settlement may require different policies."}
    correctAnswerId: A
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "7.Rounding/Rounding.md", anchor: "financial-rounding-policy"}
    apiRelated: {controller: "", methodSignature: ""}

  - id: bigdecimal-equals-compareto
    question: "How do `new BigDecimal(\"1.0\")` and `new BigDecimal(\"1.00\")` compare?"
    answers:
      - {id: A, answer: "Both equals and compareTo report unequal magnitude.", explanation: "Incorrect. BigDecimal equality includes representation scale; natural numeric comparison ignores that scale difference."}
      - {id: B, answer: "`equals` is false because scale differs, while `compareTo` returns 0 for equal numerical magnitude.", explanation: "Correct. BigDecimal equality includes representation scale; natural numeric comparison ignores that scale difference."}
      - {id: C, answer: "equals is true and compareTo is nonzero.", explanation: "Incorrect. BigDecimal equality includes representation scale; natural numeric comparison ignores that scale difference."}
      - {id: D, answer: "Neither method can compare different scales.", explanation: "Incorrect. BigDecimal equality includes representation scale; natural numeric comparison ignores that scale difference."}
    correctAnswerId: B
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "8.BigDecimalComparison/BigDecimalComparison.md", anchor: "big-decimal-equals"}
    apiRelated: {controller: "BigDecimalController", methodSignature: "comparison()"}

  - id: random-seed-reproducible
    question: "Why is a fixed seed useful for `Random` in tests?"
    answers:
      - {id: A, answer: "It makes Random cryptographically secure.", explanation: "Incorrect. Ordinary PRNG output is deterministic from its seed and internal state."}
      - {id: B, answer: "It guarantees every generated number is unique.", explanation: "Incorrect. Ordinary PRNG output is deterministic from its seed and internal state."}
      - {id: C, answer: "It makes the pseudo-random sequence reproducible for the same algorithm and state evolution.", explanation: "Correct. Ordinary PRNG output is deterministic from its seed and internal state."}
      - {id: D, answer: "It disables all randomness APIs.", explanation: "Incorrect. Ordinary PRNG output is deterministic from its seed and internal state."}
    correctAnswerId: C
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "10.Random/Random.md", anchor: "pseudo-random-model"}
    apiRelated: {controller: "RandomController", methodSignature: "seededSequence()"}

  - id: secure-random-boundary
    question: "Which source should be preferred for security-sensitive tokens?"
    answers:
      - {id: A, answer: "Random with currentTimeMillis as seed.", explanation: "Incorrect. Random and ThreadLocalRandom are not designed as cryptographically strong unpredictability sources."}
      - {id: B, answer: "Math.random because it is static.", explanation: "Incorrect. Random and ThreadLocalRandom are not designed as cryptographically strong unpredictability sources."}
      - {id: C, answer: "A counter converted to hex.", explanation: "Incorrect. Random and ThreadLocalRandom are not designed as cryptographically strong unpredictability sources."}
      - {id: D, answer: "SecureRandom, because unpredictability is part of the security contract.", explanation: "Correct. Random and ThreadLocalRandom are not designed as cryptographically strong unpredictability sources."}
    correctAnswerId: D
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "11.SecureRandom/SecureRandom.md", anchor: "secure-random-purpose"}
    apiRelated: {controller: "", methodSignature: ""}

  - id: numeric-model-domain
    question: "What is the best first question when choosing between an integer type, floating-point type, and BigDecimal?"
    answers:
      - {id: A, answer: "Which type name is shortest to type?", explanation: "Incorrect. Numeric type choice should follow the domain's range, exactness, scale, and performance requirements."}
      - {id: B, answer: "Which type has the largest memory footprint?", explanation: "Incorrect. Numeric type choice should follow the domain's range, exactness, scale, and performance requirements."}
      - {id: C, answer: "What range and exactness/precision semantics does the domain require?", explanation: "Correct. Numeric type choice should follow the domain's range, exactness, scale, and performance requirements."}
      - {id: D, answer: "Can every value be converted to String first?", explanation: "Incorrect. Numeric type choice should follow the domain's range, exactness, scale, and performance requirements."}
    correctAnswerId: C
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "1.NumericModel/NumericModel.md", anchor: "numeric-type-model"}
    apiRelated: {controller: "", methodSignature: ""}

  - id: strictmath-boundary
    question: "Which statement best describes the boundary between `Math` and `StrictMath`?"
    answers:
      - {id: A, answer: "StrictMath uses arbitrary precision, so it is always exact where Math is approximate.", explanation: "Incorrect. Both APIs still operate on finite floating-point representations; StrictMath does not turn double into exact decimal arithmetic."}
      - {id: B, answer: "Math is the normal default; StrictMath is useful when more tightly specified behavior for relevant floating-point functions is needed for reproducibility.", explanation: "Correct. The boundary is about specification/reproducibility, not arbitrary precision."}
      - {id: C, answer: "StrictMath exists only for integer-overflow detection.", explanation: "Incorrect. Exact integer helpers are on Math; StrictMath concerns mathematical floating-point functions."}
      - {id: D, answer: "Math and StrictMath differ because one internally uses BigDecimal.", explanation: "Incorrect. That is not their API contract."}
    correctAnswerId: B
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "9.Math/Math.md", anchor: "strictmath-boundary"}
    apiRelated: {controller: "", methodSignature: ""}

  - id: numeric-intermediate-overflow
    question: "For `int quantity = 1_000_000; int price = 10_000; long total = quantity * price;`, what matters most?"
    answers:
      - {id: A, answer: "The multiplication runs as long because the target variable is long.", explanation: "Incorrect. The arithmetic-expression type is determined before assignment conversion."}
      - {id: B, answer: "The multiplication runs as int and may overflow before the result is widened to long.", explanation: "Correct. Widen an operand before multiplication if the operation itself must run as long."}
      - {id: C, answer: "Java automatically switches to BigInteger if needed.", explanation: "Incorrect. Primitive arithmetic never switches to an arbitrary-precision type automatically."}
      - {id: D, answer: "The expression does not compile because int cannot be assigned to long.", explanation: "Incorrect. The int result can widen to long after arithmetic has already executed."}
    correctAnswerId: B
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "1.NumericModel/NumericModel.md", anchor: "numeric-conversions"}
    apiRelated: {controller: "IntegerArithmeticController", methodSignature: "intermediateOverflow()"}

  - id: integer-abs-min-boundary
    question: "What does `Math.abs(Integer.MIN_VALUE)` return?"
    answers:
      - {id: A, answer: "Integer.MAX_VALUE.", explanation: "Incorrect. The positive counterpart 2147483648 does not fit in int."}
      - {id: B, answer: "0.", explanation: "Incorrect. Math.abs does not clamp to zero."}
      - {id: C, answer: "Integer.MIN_VALUE again because of the overflow boundary.", explanation: "Correct. MIN_VALUE has no representable positive counterpart as int."}
      - {id: D, answer: "It always throws ArithmeticException.", explanation: "Incorrect. Unchecked Math.abs does not throw here; Math.absExact does on Java 21."}
    correctAnswerId: C
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "2.IntegerOverflow/IntegerOverflow.md", anchor: "boundary-values"}
    apiRelated: {controller: "IntegerArithmeticController", methodSignature: "boundaryCases()"}

  - id: integer-divide-min-minus-one
    question: "What does `Integer.MIN_VALUE / -1` produce in ordinary int division?"
    answers:
      - {id: A, answer: "Integer.MIN_VALUE because this is the special overflow case.", explanation: "Correct. The mathematical result 2147483648 does not fit in int, so unchecked division overflows back to MIN_VALUE."}
      - {id: B, answer: "Integer.MAX_VALUE.", explanation: "Incorrect. Java does not saturate at MAX_VALUE."}
      - {id: C, answer: "ArithmeticException because of overflow.", explanation: "Incorrect. Ordinary division throws for divisor zero; Math.divideExact detects this overflow."}
      - {id: D, answer: "0.", explanation: "Incorrect. This is not truncation to zero."}
    correctAnswerId: A
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "2.IntegerOverflow/IntegerOverflow.md", anchor: "boundary-values"}
    apiRelated: {controller: "IntegerArithmeticController", methodSignature: "boundaryCases()"}

  - id: floating-signed-zero
    question: "Which statement about `+0.0` and `-0.0` is correct?"
    answers:
      - {id: A, answer: "They are always unequal with ==.", explanation: "Incorrect. 0.0 == -0.0 is true."}
      - {id: B, answer: "They compare equal with ==, but reciprocals of +0.0 and -0.0 produce infinities with different signs.", explanation: "Correct. Signed zero can affect arithmetic even though the equality operator considers the zeros equal."}
      - {id: C, answer: "Java has no negative zero.", explanation: "Incorrect. IEEE 754 includes signed zero."}
      - {id: D, answer: "Both reciprocals are always NaN.", explanation: "Incorrect. Reciprocals of signed zero produce signed infinities."}
    correctAnswerId: B
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "3.FloatingPoint/FloatingPoint.md", anchor: "nan-infinity-negative-zero"}
    apiRelated: {controller: "FloatingPointController", methodSignature: "specialValues()"}

  - id: floating-tolerance-domain
    question: "Why should one fixed epsilon not be used for every floating-point comparison?"
    answers:
      - {id: A, answer: "Because epsilon works only for float, not double.", explanation: "Incorrect. Tolerance strategies can apply to either type but must fit the domain."}
      - {id: B, answer: "Because the JVM changes epsilon between runs.", explanation: "Incorrect. Epsilon is chosen by the program."}
      - {id: C, answer: "Because acceptable error depends on magnitude, scale, and domain requirements.", explanation: "Correct. An absolute tolerance suitable at one magnitude may be meaningless at another."}
      - {id: D, answer: "Because == is always better than tolerance.", explanation: "Incorrect. Exact equality is appropriate only when exact represented-value equality is the contract."}
    correctAnswerId: C
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "3.FloatingPoint/FloatingPoint.md", anchor: "floating-point-comparison"}
    apiRelated: {controller: "FloatingPointController", methodSignature: "comparison()"}

  - id: biginteger-exact-conversion
    question: "What is the important difference between `BigInteger.intValue()` and `intValueExact()`?"
    answers:
      - {id: A, answer: "intValue may lose high-order bits; intValueExact throws if the value does not fit in int.", explanation: "Correct. Use exact conversion when fitting the primitive range is a boundary invariant."}
      - {id: B, answer: "intValue always throws while intValueExact truncates.", explanation: "Incorrect. The semantics are reversed."}
      - {id: C, answer: "The two methods are always identical.", explanation: "Incorrect. They differ when the value is outside the int range."}
      - {id: D, answer: "intValueExact automatically returns long if int is too small.", explanation: "Incorrect. It throws ArithmeticException."}
    correctAnswerId: A
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "4.BigInteger/BigInteger.md", anchor: "big-integer-operations"}
    apiRelated: {controller: "BigIntegerController", methodSignature: "conversionBoundary()"}

  - id: biginteger-remainder-vs-mod
    question: "For BigInteger value `-7` and divisor/modulus `3`, which statement is correct?"
    answers:
      - {id: A, answer: "remainder and mod must both return -1.", explanation: "Incorrect. mod produces a non-negative modular result when the modulus is positive."}
      - {id: B, answer: "remainder returns -1 while mod returns 2.", explanation: "Correct. The APIs have different contracts for a negative dividend."}
      - {id: C, answer: "mod requires a negative modulus.", explanation: "Incorrect. BigInteger.mod requires a positive modulus."}
      - {id: D, answer: "Both throw because the dividend is negative.", explanation: "Incorrect. A negative dividend is allowed; the result semantics differ."}
    correctAnswerId: B
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "4.BigInteger/BigInteger.md", anchor: "big-integer-operations"}
    apiRelated: {controller: "BigIntegerController", methodSignature: "remainderVsMod()"}

  - id: bigdecimal-valueof-after-double-arithmetic
    question: "What is true about `BigDecimal.valueOf(0.1 + 0.2)`?"
    answers:
      - {id: A, answer: "It always recovers the exact decimal intent 0.3.", explanation: "Incorrect. Floating-point arithmetic has already happened before BigDecimal is constructed."}
      - {id: B, answer: "It does not compile because valueOf cannot accept a double.", explanation: "Incorrect. BigDecimal.valueOf(double) exists."}
      - {id: C, answer: "It captures the decimal string of the already-computed double result, about 0.30000000000000004.", explanation: "Correct. BigDecimal cannot infer intent lost before construction."}
      - {id: D, answer: "It reruns the addition using decimal arithmetic.", explanation: "Incorrect. BigDecimal receives the double result after the addition."}
    correctAnswerId: C
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "5.BigDecimal/BigDecimal.md", anchor: "big-decimal-construction"}
    apiRelated: {controller: "", methodSignature: ""}

  - id: bigdecimal-multiply-scale
    question: "Without MathContext, how is the preferred scale of `BigDecimal.multiply` generally determined?"
    answers:
      - {id: A, answer: "It is always 2.", explanation: "Incorrect. Scale is not hard-coded to money semantics."}
      - {id: B, answer: "It is left scale + right scale.", explanation: "Correct. Multiplication adds the preferred scales of the operands."}
      - {id: C, answer: "It is max(left scale, right scale).", explanation: "Incorrect. That rule better describes add/subtract preferred scale."}
      - {id: D, answer: "It is always 0.", explanation: "Incorrect. BigDecimal multiplication carries scale semantics from its operands."}
    correctAnswerId: B
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "5.BigDecimal/BigDecimal.md", anchor: "big-decimal-arithmetic"}
    apiRelated: {controller: "BigDecimalController", methodSignature: "divisionAndRounding()"}

  - id: mathcontext-vs-setscale
    question: "What best distinguishes `setScale(2, mode)` from `MathContext(4, mode)`?"
    answers:
      - {id: A, answer: "They always do the same thing.", explanation: "Incorrect. Scale and precision are different controls."}
      - {id: B, answer: "setScale limits total significant digits while MathContext only formats output.", explanation: "Incorrect. The roles are reversed."}
      - {id: C, answer: "setScale controls scale/decimal places; MathContext controls total significant-digit precision of an operation.", explanation: "Correct. This is the core distinction between scale and precision."}
      - {id: D, answer: "MathContext works only with double.", explanation: "Incorrect. MathContext is a central BigDecimal API."}
    correctAnswerId: C
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "6.PrecisionScale/PrecisionScale.md", anchor: "math-context"}
    apiRelated: {controller: "BigDecimalController", methodSignature: "mathContextVsScale()"}

  - id: rounding-negative-half-tie
    question: "When rounding `-2.5` to an integer, which pair is correct?"
    answers:
      - {id: A, answer: "HALF_UP → -3, HALF_DOWN → -2.", explanation: "Correct. HALF_UP moves away from zero on a tie; HALF_DOWN moves toward zero."}
      - {id: B, answer: "HALF_UP → -2, HALF_DOWN → -3.", explanation: "Incorrect. The directions are reversed."}
      - {id: C, answer: "Both always remain -2.5.", explanation: "Incorrect. Target scale 0 requires rounding."}
      - {id: D, answer: "Both throw because the value is negative.", explanation: "Incorrect. Negative values are rounded normally."}
    correctAnswerId: A
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "7.Rounding/Rounding.md", anchor: "rounding-modes"}
    apiRelated: {controller: "BigDecimalController", methodSignature: "roundingModes()"}

  - id: rounding-double-rounding
    question: "Why can double rounding change the final result?"
    answers:
      - {id: A, answer: "Each rounding step can discard information, so later rounding receives a changed input.", explanation: "Correct. Intermediate rounding can alter the next rounding boundary."}
      - {id: B, answer: "Because BigDecimal is mutable.", explanation: "Incorrect. BigDecimal is immutable."}
      - {id: C, answer: "Because RoundingMode is random.", explanation: "Incorrect. Rounding modes are deterministic."}
      - {id: D, answer: "Double rounding is always mathematically identical to rounding once.", explanation: "Incorrect. Values such as 2.445 can diverge depending on the sequence."}
    correctAnswerId: A
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "7.Rounding/Rounding.md", anchor: "rounding-at-boundaries"}
    apiRelated: {controller: "", methodSignature: ""}

  - id: bigdecimal-hashset-treeset
    question: "If `1.0` and `1.00` are added to both `HashSet<BigDecimal>` and `TreeSet<BigDecimal>`, what can happen?"
    answers:
      - {id: A, answer: "Both sets always have size 2.", explanation: "Incorrect. TreeSet uses natural ordering, where compareTo can return 0."}
      - {id: B, answer: "HashSet has size 1 and TreeSet has size 2.", explanation: "Incorrect. The semantics are reversed in this case."}
      - {id: C, answer: "HashSet has size 2 and TreeSet has size 1.", explanation: "Correct. HashSet uses scale-sensitive equals/hashCode; TreeSet uses compareTo."}
      - {id: D, answer: "Both throw because the scales differ.", explanation: "Incorrect. Both collections accept these values."}
    correctAnswerId: C
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "8.BigDecimalComparison/BigDecimalComparison.md", anchor: "big-decimal-collections"}
    apiRelated: {controller: "BigDecimalController", methodSignature: "collectionSemantics()"}

  - id: math-floor-division-negative
    question: "For `-7` and `3`, what are `-7 / 3` and `Math.floorDiv(-7, 3)` respectively?"
    answers:
      - {id: A, answer: "-2 and -3.", explanation: "Correct. / truncates toward zero while floorDiv moves toward negative infinity."}
      - {id: B, answer: "-3 and -2.", explanation: "Incorrect. The semantics are reversed."}
      - {id: C, answer: "Both are -2.", explanation: "Incorrect. They differ when the negative result is not integral."}
      - {id: D, answer: "Both are -3.", explanation: "Incorrect. Ordinary integer division truncates toward zero."}
    correctAnswerId: A
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "9.Math/Math.md", anchor: "math-core-functions"}
    apiRelated: {controller: "MathController", methodSignature: "floorDivision()"}

  - id: math-abs-exact-boundary
    question: "On Java 21, what does `Math.absExact(Integer.MIN_VALUE)` do?"
    answers:
      - {id: A, answer: "Returns Integer.MAX_VALUE.", explanation: "Incorrect. There is no representable positive counterpart."}
      - {id: B, answer: "Returns Integer.MIN_VALUE like Math.abs.", explanation: "Incorrect. The exact variant detects overflow."}
      - {id: C, answer: "Throws ArithmeticException.", explanation: "Correct. The exact helper turns the boundary overflow into explicit failure."}
      - {id: D, answer: "Returns long 2147483648.", explanation: "Incorrect. The int overload returns int or throws."}
    correctAnswerId: C
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "9.Math/Math.md", anchor: "exact-arithmetic-methods"}
    apiRelated: {controller: "MathController", methodSignature: "exactBoundaries()"}

  - id: threadlocalrandom-boundary
    question: "When is `ThreadLocalRandom` a better fit than an ordinary shared `Random`?"
    answers:
      - {id: A, answer: "When many threads need pseudo-random values and unnecessary contention should be avoided.", explanation: "Correct. This is a concurrency/performance boundary, not a security boundary."}
      - {id: B, answer: "When generating password-reset tokens.", explanation: "Incorrect. Security-sensitive unpredictability requires SecureRandom."}
      - {id: C, answer: "Only when the code has no threads.", explanation: "Incorrect. The API is specifically relevant to concurrent use."}
      - {id: D, answer: "Because ThreadLocalRandom is cryptographically secure.", explanation: "Incorrect. It is not a cryptographic random source."}
    correctAnswerId: A
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "10.Random/Random.md", anchor: "threadlocal-random-boundary"}
    apiRelated: {controller: "", methodSignature: ""}

  - id: random-modulo-pitfall
    question: "Why is `random.nextInt() % 100` usually worse than `random.nextInt(100)` for a 0..99 range?"
    answers:
      - {id: A, answer: "Modulo always crashes the JVM.", explanation: "Incorrect. The issue is range/sign/distribution semantics, not a crash."}
      - {id: B, answer: "Manual modulo can produce negative results and can introduce distribution mistakes; the bounded API expresses the contract directly.", explanation: "Correct. Prefer the bounded API when a bounded range is the intent."}
      - {id: C, answer: "nextInt(100) is always cryptographically secure.", explanation: "Incorrect. This is still an ordinary PRNG."}
      - {id: D, answer: "The modulo operator does not exist for int.", explanation: "Incorrect. % exists; it is simply not the better bounded-random API here."}
    correctAnswerId: B
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "10.Random/Random.md", anchor: "pseudo-random-model"}
    apiRelated: {controller: "", methodSignature: ""}

  - id: securerandom-setseed-nuance
    question: "Which statement about `SecureRandom.setSeed(...)` is most accurate?"
    answers:
      - {id: A, answer: "Every timestamp seed automatically makes the generator more secure.", explanation: "Incorrect. A timestamp is not a strong primary entropy source."}
      - {id: B, answer: "setSeed supplements state; but if a new PRNG is caller-seeded before its first self-seeding operation, the caller must provide enough entropy.", explanation: "Correct. Security depends on the entropy/state lifecycle, not only the class name."}
      - {id: C, answer: "setSeed always resets all previous entropy to exactly the supplied seed.", explanation: "Incorrect. Its contract supplements seed/state rather than behaving like a simple deterministic Random reset."}
      - {id: D, answer: "SecureRandom has no seeding concept.", explanation: "Incorrect. Seeding is central to the CSPRNG lifecycle."}
    correctAnswerId: B
    aiGenerated: true
    reviewed: false
    readmeRelated: {file: "11.SecureRandom/SecureRandom.md", anchor: "entropy-seeding"}
    apiRelated: {controller: "", methodSignature: ""}
