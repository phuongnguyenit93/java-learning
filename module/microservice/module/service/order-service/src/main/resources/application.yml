# AUTO-GENERATED MERGED YAML
# You can check at module/microservice/module/service/order-service/src/main/resources/application.yml
# Source: ORDER_SERVICE
# Dependencies: SPRING_WEB, SPRING_SWAGGER

---
spring:
  config:
    import: "optional:configserver:http://admin:123456@localhost:8888/config-server"
  cloud:
    config:
      name: order-service # Tên file cần lấy
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DATABASE_MYSQL_URI}
    username: ${DATABASE_MYSQL_USER}
    password: ${DATABASE_MYSQL_PASSWORD}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      idle-timeout: 300000
      connection-timeout: 20000
      max-lifetime: 1800000
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  application:
    name: ${PROJECT_NAME}
swagger:
  enabled: ${BUILD_SWAGGER:}
  languages: ${BUILD_SWAGGER_LANGUAGE_LIST:}
springdoc:
  swagger-ui:
    enabled: ${swagger.enabled}
    docExpansion:
    - none
  api-docs:
    enabled: ${swagger.enabled}
server:
  port: ${PORT}
  forward-headers-strategy: framework
  servlet:
    context-path: /${CONTEXT_PATH}

integration:
  client:
    inventory: ${INVENTORY_CLIENT_URL}

eureka:
  instance:
    # Thông báo cho Admin/Eureka biết đường dẫn chính xác
    status-page-url-path: ${server.servlet.context-path}/actuator/info
    health-check-url-path: ${server.servlet.context-path}/actuator/health
    # Quan trọng: Metadata giúp Admin Server tự nối chuỗi URL
    metadata-map:
      # Đây là dòng then chốt:
      management.context-path: ${server.servlet.context-path}/actuator
---
spring:
  config:
    activate:
      on-profile: dev
---
spring:
  config:
    activate:
      on-profile: loc
---
spring:
  config:
    activate:
      on-profile: prd
swagger:
  enabled: false
