# AUTO-GENERATED MERGED YAML
# You can check at module/microservice/module/infrastructure/api-gateway/src/main/resources/application.yml
# Source: API_GATEWAY
# Dependencies: SPRING_WEB, SPRING_SWAGGER

---
spring:
  config:
    activate:
      on-profile: dev
---
spring:
  config:
    activate:
      on-profile: loc
---
spring:
  config:
    activate:
      on-profile: prd
swagger:
  enabled: false
---
spring:
  security:
    oauth2:
      client:
        provider:
          keycloak:
            # Đường dẫn đến Realm bạn vừa tạo
            issuer-uri: http://localhost:8180/realms/java-learning
        registration:
          keycloak:
            client-id: microservices-client
            client-secret: L6QTJY5CifzJUi5SEspT17R4FMQiuQaT
            scope: openid, profile, email
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/realms/java-learning
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:dev}
  application:
    name: ${PROJECT_NAME}
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
swagger:
  enabled: ${BUILD_SWAGGER:}
  languages: ${BUILD_SWAGGER_LANGUAGE_LIST:}
springdoc:
  swagger-ui:
    enabled: ${swagger.enabled}
    docExpansion:
    - none
    path: /swagger-ui.html
  api-docs:
    enabled: ${swagger.enabled}
  enable-native-support: true
  show-actuator: true
  cache:
    disabled: true

server:
  port: ${PORT}
  servlet:
    context-path: /${CONTEXT_PATH}
  session:
    cookie:
      name: GATEWAY_SESSION_ID

eureka:
  instance:
    hostname: ${EUREKA_SERVER:localhost}
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
